const models = require('../src')

const { listFolders } = require('connultis').fs

// Create Companies

// For each company, create events and locals and map to locals set lo

//
/*
    For each event
        Lecture
            Lecture Type - If exists with the same name, use existent
        
*/
